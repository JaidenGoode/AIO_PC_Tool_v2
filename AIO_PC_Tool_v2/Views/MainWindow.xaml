<Window x:Class="AIO_PC_Tool_v2.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AIO_PC_Tool_v2.ViewModels"
        mc:Ignorable="d"
        Title="JGoode's AIO PC Tool v2" 
        Height="800" Width="1280"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundDark}"
        TextElement.Foreground="{DynamicResource TextPrimary}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="Segoe UI"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="12"/>
    </WindowChrome.WindowChrome>
    
    <Border CornerRadius="12" BorderBrush="{DynamicResource BorderSubtle}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="{DynamicResource BackgroundCard}" CornerRadius="12,12,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- App Icon & Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                        <Border Width="24" Height="24" CornerRadius="6" Background="{DynamicResource AccentPrimary}" Margin="0,0,10,0">
                            <TextBlock Text="A" FontSize="12" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="JGoode's AIO PC Tool" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text=" v4.0" FontSize="12" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                    
                    <!-- Window Controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Click="MinimizeButton_Click" ToolTip="Minimize" Style="{StaticResource GhostButton}" Padding="14,8">
                            <TextBlock Text="—" FontSize="12" Foreground="{DynamicResource TextSecondary}"/>
                        </Button>
                        <Button Click="MaximizeButton_Click" ToolTip="Maximize" Style="{StaticResource GhostButton}" Padding="14,8">
                            <TextBlock Text="☐" FontSize="12" Foreground="{DynamicResource TextSecondary}"/>
                        </Button>
                        <Button Click="CloseButton_Click" ToolTip="Close" Style="{StaticResource GhostButton}" Padding="14,8">
                            <TextBlock Text="✕" FontSize="12" Foreground="{DynamicResource ErrorRed}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Main Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Sidebar Navigation -->
                <Border Grid.Column="0" Background="{DynamicResource BackgroundSidebar}" Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0">
                            <TextBlock Text="NAVIGATION" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" Margin="12,8,0,16" Opacity="0.7"/>
                            
                            <!-- Dashboard -->
                            <RadioButton x:Name="NavDashboard" IsChecked="True" GroupName="Navigation" Click="NavButton_Click" Tag="Dashboard">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border x:Name="border" Background="Transparent" CornerRadius="10" Padding="14,12" Margin="0,2" Cursor="Hand">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📊" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Text="Dashboard" Margin="12,0,0,0" Foreground="{DynamicResource TextSecondary}" FontWeight="Medium" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentPrimary}"/>
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsChecked" Value="False"/>
                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BackgroundCardHover}"/>
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>
                            
                            <!-- Tweaks -->
                            <RadioButton x:Name="NavTweaks" GroupName="Navigation" Click="NavButton_Click" Tag="Tweaks">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border x:Name="border" Background="Transparent" CornerRadius="10" Padding="14,12" Margin="0,2" Cursor="Hand">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="⚙" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Text="Tweaks" Margin="12,0,0,0" Foreground="{DynamicResource TextSecondary}" FontWeight="Medium" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentPrimary}"/>
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsChecked" Value="False"/>
                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BackgroundCardHover}"/>
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>
                            
                            <!-- Cleaner -->
                            <RadioButton x:Name="NavCleaner" GroupName="Navigation" Click="NavButton_Click" Tag="Cleaner">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border x:Name="border" Background="Transparent" CornerRadius="10" Padding="14,12" Margin="0,2" Cursor="Hand">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🧹" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Text="Cleaner" Margin="12,0,0,0" Foreground="{DynamicResource TextSecondary}" FontWeight="Medium" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentPrimary}"/>
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsChecked" Value="False"/>
                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BackgroundCardHover}"/>
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>
                            
                            <!-- DNS Manager -->
                            <RadioButton x:Name="NavDns" GroupName="Navigation" Click="NavButton_Click" Tag="DNS">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border x:Name="border" Background="Transparent" CornerRadius="10" Padding="14,12" Margin="0,2" Cursor="Hand">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🌐" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Text="DNS Manager" Margin="12,0,0,0" Foreground="{DynamicResource TextSecondary}" FontWeight="Medium" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentPrimary}"/>
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsChecked" Value="False"/>
                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BackgroundCardHover}"/>
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>
                            
                            <!-- Restore Points -->
                            <RadioButton x:Name="NavRestore" GroupName="Navigation" Click="NavButton_Click" Tag="Restore">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border x:Name="border" Background="Transparent" CornerRadius="10" Padding="14,12" Margin="0,2" Cursor="Hand">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🔄" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Text="Restore Points" Margin="12,0,0,0" Foreground="{DynamicResource TextSecondary}" FontWeight="Medium" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentPrimary}"/>
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsChecked" Value="False"/>
                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BackgroundCardHover}"/>
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>
                            
                            <!-- Utilities -->
                            <RadioButton x:Name="NavUtilities" GroupName="Navigation" Click="NavButton_Click" Tag="Utilities">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border x:Name="border" Background="Transparent" CornerRadius="10" Padding="14,12" Margin="0,2" Cursor="Hand">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🔧" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Text="Utilities" Margin="12,0,0,0" Foreground="{DynamicResource TextSecondary}" FontWeight="Medium" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentPrimary}"/>
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsChecked" Value="False"/>
                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BackgroundCardHover}"/>
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>
                            
                            <Border Height="1" Background="{DynamicResource BorderSubtle}" Margin="0,16"/>
                            
                            <!-- Settings -->
                            <RadioButton x:Name="NavSettings" GroupName="Navigation" Click="NavButton_Click" Tag="Settings">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border x:Name="border" Background="Transparent" CornerRadius="10" Padding="14,12" Margin="0,2" Cursor="Hand">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="⚡" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Text="Settings" Margin="12,0,0,0" Foreground="{DynamicResource TextSecondary}" FontWeight="Medium" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentPrimary}"/>
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsChecked" Value="False"/>
                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BackgroundCardHover}"/>
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>
                        </StackPanel>
                        
                        <!-- Footer -->
                        <StackPanel Grid.Row="1" Margin="0,12,0,0">
                            <Border Background="{DynamicResource BackgroundCard}" CornerRadius="10" Padding="14">
                                <StackPanel>
                                    <TextBlock Text="System Status" FontWeight="SemiBold" FontSize="12" Foreground="{DynamicResource TextPrimary}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <Ellipse Width="8" Height="8" Fill="{DynamicResource SuccessGreen}" VerticalAlignment="Center"/>
                                        <TextBlock Text="All systems operational" FontSize="11" Foreground="{DynamicResource TextSecondary}" Margin="8,0,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Main Content Area -->
                <Border Grid.Column="1" Background="{DynamicResource BackgroundDark}" Padding="28">
                    <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
