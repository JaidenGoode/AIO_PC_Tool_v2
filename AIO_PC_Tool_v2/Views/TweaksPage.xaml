<Page x:Class="AIO_PC_Tool_v2.Views.TweaksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Tweaks" Background="#0D0D0D">
    
    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="System Tweaks" FontSize="28" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Optimize your Windows system with safe, reversible tweaks" FontSize="14" Foreground="#A3A3A3" Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- Controls -->
        <Border Grid.Row="1" Background="#1A1A1A" CornerRadius="10" Padding="16" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Category:" Foreground="#A3A3A3" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <ComboBox x:Name="CategoryFilter" Width="180" SelectionChanged="CategoryFilter_Changed" Background="#252525" Foreground="White" BorderThickness="0" Padding="12,8">
                        <ComboBoxItem Content="All" IsSelected="True"/>
                        <ComboBoxItem Content="Privacy"/>
                        <ComboBoxItem Content="Performance"/>
                        <ComboBoxItem Content="Gaming"/>
                        <ComboBoxItem Content="Visual"/>
                        <ComboBoxItem Content="Network"/>
                        <ComboBoxItem Content="Power"/>
                        <ComboBoxItem Content="Security"/>
                        <ComboBoxItem Content="Storage"/>
                    </ComboBox>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Apply All Shown" Click="ApplyAll_Click" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
                    <Button Content="Revert All Shown" Click="RevertAll_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Tweaks List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="TweaksList"/>
        </ScrollViewer>
    </Grid>
</Page>
