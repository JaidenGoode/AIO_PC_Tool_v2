<Page x:Class="AIO_PC_Tool_v2.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:vm="clr-namespace:AIO_PC_Tool_v2.ViewModels"
      Title="Settings" Background="Transparent">
    
    <Page.DataContext>
        <vm:SettingsViewModel/>
    </Page.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Settings" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
            <TextBlock Text="Customize the appearance of the application" FontSize="14" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0"/>
        </StackPanel>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="600">
                
                <!-- Theme Section -->
                <Border Background="{DynamicResource BackgroundCard}" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="APPEARANCE" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,16"/>
                        
                        <!-- Dark Mode Toggle -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Dark Mode" FontWeight="SemiBold" FontSize="15" Foreground="{DynamicResource TextPrimary}"/>
                                <TextBlock Text="Use dark theme for the application" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0"/>
                            </StackPanel>
                            
                            <ToggleButton Grid.Column="1" 
                                          IsChecked="{Binding IsDarkMode}"
                                          Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                          VerticalAlignment="Center"/>
                        </Grid>
                        
                        <!-- Accent Color -->
                        <TextBlock Text="Accent Color" FontWeight="SemiBold" FontSize="15" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,12"/>
                        <ItemsControl ItemsSource="{Binding AccentColors}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <RadioButton GroupName="AccentColor" 
                                                 IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                 Margin="0,0,8,8"
                                                 Command="{Binding DataContext.SelectAccentCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                 CommandParameter="{Binding}">
                                        <RadioButton.Template>
                                            <ControlTemplate TargetType="RadioButton">
                                                <Border x:Name="border" Width="40" Height="40" CornerRadius="20" 
                                                        BorderThickness="3" BorderBrush="Transparent" Cursor="Hand">
                                                    <Ellipse Width="30" Height="30">
                                                        <Ellipse.Fill>
                                                            <SolidColorBrush Color="{Binding Converter={x:Static materialDesign:ColorConverter.Default}}"/>
                                                        </Ellipse.Fill>
                                                    </Ellipse>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource TextPrimary}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </RadioButton.Template>
                                    </RadioButton>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                
                <!-- About Section -->
                <Border Background="{DynamicResource BackgroundCard}" CornerRadius="12" Padding="20">
                    <StackPanel>
                        <TextBlock Text="ABOUT" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,16"/>
                        
                        <TextBlock Text="JGoode's AIO PC Tool v2" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text="Version 4.0.0" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0"/>
                        
                        <TextBlock Text="A professional Windows optimization tool built with WPF and .NET 8. Features safe, reversible system tweaks with detailed descriptions and warnings."
                                   FontSize="13" 
                                   Foreground="{DynamicResource TextSecondary}" 
                                   TextWrapping="Wrap" 
                                   Margin="0,16,0,20"/>
                        
                        <!-- Features list -->
                        <Border Background="{DynamicResource BackgroundInput}" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="Built with" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,8"/>
                                <TextBlock Text="WPF (.NET 8)" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text="Material Design in XAML" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text="LibreHardwareMonitor" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text="CommunityToolkit.Mvvm" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                            </StackPanel>
                        </Border>
                        
                        <TextBlock FontSize="12" Foreground="{DynamicResource TextMuted}">
                            <Run Text="Created by JaidenGoode"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
