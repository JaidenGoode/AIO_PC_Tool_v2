<Page x:Class="AIO_PC_Tool_v2.Views.RestorePointsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Restore Points" Background="#0D0D0D">
    
    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Restore Points" FontSize="28" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Create and manage Windows System Restore points" FontSize="14" Foreground="#A3A3A3" Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- Create New -->
        <Border Grid.Row="1" Background="#1A1A1A" CornerRadius="12" Padding="20" Margin="0,0,0,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="PointName" Grid.Column="0" Background="#252525" Foreground="White" BorderThickness="0" Padding="12" FontSize="14"/>
                <Button Grid.Column="1" Content="Create Restore Point" Click="Create_Click" Style="{StaticResource PrimaryButtonStyle}" Margin="16,0,0,0"/>
            </Grid>
        </Border>
        
        <!-- Existing Points -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="EXISTING RESTORE POINTS" FontSize="11" FontWeight="Bold" Foreground="#737373"/>
                <Button Content="Refresh" Click="Refresh_Click" Style="{StaticResource SecondaryButtonStyle}" Margin="16,0,0,0" Padding="12,6"/>
            </StackPanel>
            
            <ScrollViewer Grid.Row="1">
                <StackPanel x:Name="PointsList"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
